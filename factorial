package main
import "fmt"
func main() {
    var num int 
    fmt.Println("Enter number") 
    fmt.Scanln( & num) 
    fact: = 1
    for i: = 1;i <= num; i++{
        fact = fact * i
    }
    fmt.Println("Factorial of", num, "is", fact)
}
